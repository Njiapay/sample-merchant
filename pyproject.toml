[project]
name = "sample-app"
version = "0.1.0"
description = "A sample application to demonstrate NjiaPay usage"
readme = "README.md"
authors = [
    { name = "Caitlin Cannon", email = "caitlin@njiapay.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.0",
    "requests>=2.34.2",
    "ruff>=0.16.3",
]

[project.scripts]
sample_app = "sample_app:main"
